import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: 300,
    maxWidth: "80%",
    padding: 20,
    alignItems: "center",
    shadowColor: "black",
    backgroundColor: "white",
    shadowOpacity: 10,
    shadowRadius: 0.3,
    elevation: 5,
    borderRadius: 10,
  },
});
